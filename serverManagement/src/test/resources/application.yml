spring:
  datasource:
    url: jdbc:h2:mem:servertestdb;DB_CLOSE_DELAY=-1
    password: test
    username: test
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
